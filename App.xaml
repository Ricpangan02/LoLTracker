<Application x:Class="LoLTracker.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:LoLTracker.Converters"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Simple dark-ish resource dictionary -->
        <ResourceDictionary>
            <SolidColorBrush x:Key="WindowBackground"
                             Color="#FF121212"/>
            <SolidColorBrush x:Key="PanelBackground"
                             Color="#FF1E1E1E"/>
            <SolidColorBrush x:Key="Accent"
                             Color="#FFEE7A1A"/>
            <Style TargetType="Window">
                <Setter Property="Background"
                        Value="{StaticResource WindowBackground}"/>
                <Setter Property="Foreground"
                        Value="White"/>
            </Style>
            <Style TargetType="Button">
                <Setter Property="Background"
                        Value="{StaticResource Accent}"/>
                <Setter Property="Foreground"
                        Value="White"/>
                <Setter Property="Margin"
                        Value="4"/>
                <Setter Property="Padding"
                        Value="6,4"/>
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="Background"
                        Value="#FF2B2B2B"/>
                <Setter Property="Foreground"
                        Value="White"/>
                <Setter Property="Margin"
                        Value="4"/>
            </Style>
            <Style TargetType="ComboBox">
                <Setter Property="Background"
                        Value="#FF2B2B2B"/>
                <Setter Property="Foreground"
                        Value="White"/>
            </Style>

            <conv:BoolToResultConverter x:Key="BoolToResultConverter"/>

        </ResourceDictionary>
    </Application.Resources>
</Application>
